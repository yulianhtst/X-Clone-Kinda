// const validate = (target, value) => {
//   const actions = {
//     email: () => {
//       const pattern = new RegExp("([a-zA-Z0-9]+)@([a-zA-Z]+)\\.([a-zA-Z]+)");
//       const isEmailValid = pattern.test(value);
//       return isEmailValid;
//     },

//   };

//   return actions[target];
// };

// export const email = validate.bind({}, "email");
